{
  "name": "@tre/client",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.4.0",
  "engines": {
    "node": "^22.2.0"
  },
  "scripts": {
    "build": "vite build",
    "exec": "node --enable-source-maps dist/main.js",
    "check": "tsc && biome check && biome format"
  },
  "dependencies": {},
  "devDependencies": {
    "@tre/api": "workspace:*"
  }
}
