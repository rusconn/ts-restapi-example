{
  "name": "@tre/client",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.16.1",
  "engines": {
    "node": "^24.10.0"
  },
  "scripts": {
    "build": "vite build",
    "exec": "node --enable-source-maps dist/main.js",
    "check": "tsc && biome check",
    "checkgo": "tsgo && biome check"
  },
  "dependencies": {},
  "devDependencies": {
    "@tre/api": "workspace:*"
  }
}
