{
  "name": "ts-restapi-example",
  "license": "MIT",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.4.0",
  "engines": {
    "node": "^22.2.0"
  },
  "scripts": {
    "up": "pnpm -r --stream run up",
    "down": "pnpm -r --stream run down",
    "dev": "pnpm -r --stream run dev",
    "build": "pnpm -r --stream run build",
    "start": "pnpm -r --stream run start",
    "test": "pnpm -r --stream run test",
    "check": "pnpm -r --stream run check",
    "checkgo": "pnpm -r --stream run checkgo"
  },
  "dependencies": {
    "hono": "^4.6.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "@dotenvx/dotenvx": "^1.14.1",
    "@typescript/native-preview": "7.0.0-dev.20250605.1",
    "prettier": "^3.3.2",
    "typescript": "^5.6.2",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  },
  "pnpm": {
    "overrides": {
      "micromatch@<4.0.8": ">=4.0.8"
    },
    "onlyBuiltDependencies": [
      "@biomejs/biome",
      "@prisma/engines",
      "esbuild",
      "prisma"
    ]
  }
}
